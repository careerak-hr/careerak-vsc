# 🔄 Backend دائم العمل - قاعدة إلزامية

## ⚠️ قاعدة مهمة جداً (محفوظة في project-standards.md)

**جميع السيرفرات يجب أن تعمل بشكل دائم وتلقائي - ممنوع التشغيل اليدوي**

## ✅ الطريقة الصحيحة الوحيدة

```bash
# تشغيل Backend (دائم)
cd backend
npm run pm2:start

# التحقق من الحالة
npm run pm2:status

# عرض السجلات
npm run pm2:logs

# إعادة التشغيل
npm run pm2:restart

# إيقاف (نادراً)
npm run pm2:stop
```

## ❌ الطرق الممنوعة منعاً باتاً

```bash
# ❌ ممنوع - تشغيل يدوي مؤقت
npm run dev
npm start
node server.js
nodemon server.js

# هذه الطرق ممنوعة لأنها:
# - تتوقف عند إغلاق Terminal
# - لا تعيد التشغيل عند الأخطاء
# - لا تبدأ تلقائياً مع Windows
# - غير مناسبة للإنتاج
```

## 📊 الحالة الحالية

```
✅ Backend يعمل الآن بشكل دائم
✅ URL: http://localhost:5000
✅ PM2 Process: careerak-backend
✅ Auto-restart: مفعّل
✅ Saved: نعم (يبدأ تلقائياً مع Windows)
```

## 🔧 الأوامر المتاحة

| الأمر | الوصف |
|------|-------|
| `npm run pm2:start` | تشغيل Backend |
| `npm run pm2:stop` | إيقاف Backend |
| `npm run pm2:restart` | إعادة تشغيل Backend |
| `npm run pm2:delete` | حذف Process |
| `npm run pm2:logs` | عرض السجلات |
| `npm run pm2:status` | عرض الحالة |
| `npm run pm2:monit` | مراقبة الأداء |

## 📝 ملاحظات مهمة

1. ✅ Backend يعمل الآن 24/7
2. ✅ يعيد التشغيل تلقائياً عند الأخطاء
3. ✅ يبدأ تلقائياً مع Windows
4. ✅ السجلات محفوظة في `backend/logs/`
5. ❌ لا تستخدم `npm run dev` أبداً

## 🎯 الفوائد

- ✅ **استقرار**: السيرفر لا يتوقف أبداً
- ✅ **موثوقية**: إعادة تشغيل تلقائي عند الأخطاء
- ✅ **راحة**: يبدأ تلقائياً مع Windows
- ✅ **مراقبة**: سجلات منظمة وسهلة الوصول
- ✅ **أداء**: إدارة الذاكرة والموارد

## 📚 التوثيق

- 📄 `.kiro/steering/project-standards.md` - القاعدة محفوظة هنا
- 📄 `docs/BACKEND_NOW_RUNNING.md` - دليل شامل
- 📄 `docs/BACKEND_PERMANENT_RUNNING.md` - الإعداد الدائم
- 📄 `backend/ecosystem.config.js` - ملف التكوين

## 🚨 تذكير

**إذا احتجت تشغيل Backend في أي وقت:**
```bash
cd backend
npm run pm2:start
```

**وليس:**
```bash
npm run dev  ❌ ممنوع
npm start    ❌ ممنوع
```

---

**تاريخ الإنشاء**: 2026-02-27  
**الحالة**: ✅ Backend يعمل الآن  
**القاعدة**: محفوظة في project-standards.md
