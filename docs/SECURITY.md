# 🔒 الأمان والخصوصية في نظام Careerak

## 1️⃣ حماية البيانات

### تشفير كلمات المرور
- ✅ استخدام `bcryptjs` لتشفير كلمات المرور
- ✅ لا يتم حفظ كلمات المرور بصيغة نصية
- ✅ كل كلمة مرور لها salt فريد

### توكن JWT
- ✅ توكن آمن للجلسات
- ✅ صلاحية محدودة (7 أيام)
- ✅ يتم التحقق من التوكن في كل طلب

### تخزين الصور
- ✅ التحقق من نوع الملف
- ✅ حد أقصى لحجم الملف (5MB)
- ✅ تشفير أسماء الملفات
- ✅ تخزين منفصل عن البيانات الحساسة

## 2️⃣ معايير الأمان

```javascript
// تشفير كلمات المرور
const salt = await bcrypt.genSalt(10);
const hashedPassword = await bcrypt.hash(password, salt);

// التحقق من الصور
const allowedMimes = ['image/jpeg', 'image/png', 'image/gif'];
const maxSize = 5 * 1024 * 1024; // 5MB

// توكن JWT
const token = jwt.sign(
  { id: user._id, role: user.role },
  process.env.JWT_SECRET,
  { expiresIn: '7d' }
);
```

## 3️⃣ حماية الاتصالات

### HTTPS في الإنتاج
```bash
# يجب تفعيل HTTPS عند النشر
# استخدم Let's Encrypt للشهادات المجانية
```

### CORS Protection
```javascript
const cors = require('cors');
app.use(cors());
```

## 4️⃣ سياسة الخصوصية

جميع بيانات المستخدمين:
- ✅ لا تُشارك مع أطراف ثالثة
- ✅ محفوظة في قاعدة بيانات آمنة
- ✅ يمكن حذفها بناءً على طلب المستخدم
- ✅ تُستخدم فقط لأغراض النظام

## 5️⃣ متطلبات قانونية

- ✅ قبول سياسة الخصوصية عند التسجيل
- ✅ حق المستخدم في الوصول لبياناته
- ✅ حق المستخدم في حذف حسابه
- ✅ حق المستخدم في تصحيح البيانات

## 6️⃣ تحديثات الأمان المستقبلية

- [ ] مصادقة ثنائية (2FA)
- [ ] تشفير البيانات الحساسة في قاعدة البيانات
- [ ] تسجيل جميع محاولات الدخول
- [ ] الكشف عن الأنشطة المريبة
- [ ] نسخ احتياطية منتظمة

## 7️⃣ تعليمات للمسؤولين

### المتغيرات البيئية الآمنة
```env
JWT_SECRET=string_طويل_عشوائي_قوي
MONGODB_URI=mongodb+srv://username:password@host
PORT=5000
NODE_ENV=production
```

### النسخ الاحتياطية
```bash
# نسخ احتياطية يومية من قاعدة البيانات
mongodump --uri="mongodb://..." --out=/backups/
```

---

**آخر تحديث:** يناير 2026
