# 🚀 خيارات تشغيل Backend - Careerak

## 📋 الخيارات المتاحة

لديك 3 طرق لتشغيل Backend بشكل دائم:

---

## 🎯 الخيار 1: PM2 (الأفضل - موصى به)

### المميزات:
- ✅ تشغيل دائم 24/7
- ✅ إعادة تشغيل تلقائي عند الأخطاء
- ✅ بدء مع Windows
- ✅ مراقبة الأداء
- ✅ إدارة Logs

### التثبيت:
```bash
npm install -g pm2
```

**ملاحظة:** قد تحتاج تشغيل Command Prompt كـ **Administrator**

### التشغيل:
```bash
# الطريقة 1: ملف .bat
start-backend.bat

# الطريقة 2: npm script
npm run pm2:start

# الطريقة 3: PM2 مباشرة
pm2 start ecosystem.config.js
pm2 save
```

### الإدارة:
```bash
pm2 status              # الحالة
pm2 logs                # Logs
pm2 restart careerak-backend  # إعادة تشغيل
pm2 stop careerak-backend     # إيقاف
```

### بدء التشغيل مع Windows:
```bash
pm2 startup
pm2 save
```

---

## 🎯 الخيار 2: تشغيل بسيط (سهل)

### المميزات:
- ✅ سهل جداً
- ✅ لا يحتاج تثبيت إضافي
- ⚠️ يحتاج نافذة مفتوحة
- ❌ لا إعادة تشغيل تلقائي

### التشغيل:
```bash
start-backend-simple.bat
```

**ملاحظة:** لا تغلق النافذة! Backend سيتوقف إذا أغلقت النافذة.

### الإيقاف:
- أغلق النافذة
- أو اضغط `Ctrl+C`
- أو استخدم: `stop-backend-simple.bat`

---

## 🎯 الخيار 3: تشغيل مخفي (في الخلفية)

### المميزات:
- ✅ يعمل في الخلفية (مخفي)
- ✅ لا يحتاج نافذة مفتوحة
- ✅ سهل الاستخدام
- ❌ لا إعادة تشغيل تلقائي

### التشغيل:
**Double-click على:**
```
start-backend-hidden.vbs
```

سترى رسالة:
```
✅ Backend يعمل الآن في الخلفية!
🌐 http://localhost:5000
📋 Logs: backend\logs\backend.log
```

### الإيقاف:
1. افتح **Task Manager** (Ctrl+Shift+Esc)
2. ابحث عن `node.exe`
3. اضغط **End Task**

**أو استخدم:**
```bash
stop-backend-simple.bat
```

---

## 📊 مقارنة الخيارات

| الميزة | PM2 | بسيط | مخفي |
|--------|-----|------|------|
| سهولة الاستخدام | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| تشغيل دائم | ✅ | ⚠️ | ⚠️ |
| إعادة تشغيل تلقائي | ✅ | ❌ | ❌ |
| بدء مع Windows | ✅ | ❌ | ⚠️ |
| مراقبة الأداء | ✅ | ❌ | ❌ |
| إدارة Logs | ✅ | ⚠️ | ⚠️ |
| يحتاج نافذة مفتوحة | ❌ | ✅ | ❌ |

---

## 🎯 أيهما أختار؟

### للإنتاج (Production):
**استخدم PM2** ✅
- تشغيل دائم
- إعادة تشغيل تلقائي
- مراقبة كاملة

### للتطوير (Development):
**استخدم البسيط** ✅
- سهل وسريع
- ترى Logs مباشرة
- سهل الإيقاف

### للاستخدام اليومي:
**استخدم المخفي** ✅
- يعمل في الخلفية
- لا يشغل نافذة
- سهل الاستخدام

---

## 🔧 حل مشكلة PM2

### المشكلة: PM2 لا يعمل بعد التثبيت

**الحل 1: أعد فتح Command Prompt**
```bash
# أغلق Command Prompt الحالي
# افتح Command Prompt جديد كـ Administrator
# جرّب مرة أخرى
pm2 --version
```

**الحل 2: استخدم npm scripts**
```bash
npm run pm2:start
npm run pm2:status
```

**الحل 3: استخدم npx**
```bash
npx pm2 start ecosystem.config.js
npx pm2 status
```

**الحل 4: استخدم الخيار البديل**
```bash
# استخدم التشغيل البسيط أو المخفي
start-backend-simple.bat
# أو
start-backend-hidden.vbs
```

---

## 📋 الملفات المتاحة

### PM2:
- `start-backend.bat` - تشغيل مع PM2
- `stop-backend.bat` - إيقاف PM2
- `restart-backend.bat` - إعادة تشغيل PM2
- `view-logs.bat` - عرض Logs PM2
- `ecosystem.config.js` - تكوين PM2

### بسيط:
- `start-backend-simple.bat` - تشغيل بسيط
- `stop-backend-simple.bat` - إيقاف

### مخفي:
- `start-backend-hidden.vbs` - تشغيل في الخلفية

---

## 🧪 الاختبار

### بعد التشغيل (أي طريقة):

```bash
# اختبار API
curl http://localhost:5000/users

# أو افتح المتصفح
http://localhost:5000/users
```

**يجب أن ترى رد من Backend** ✅

---

## 🎉 التوصية النهائية

### للمبتدئين:
1. جرّب **التشغيل المخفي** أولاً (double-click على .vbs)
2. إذا نجح، استخدمه يومياً
3. للإيقاف: `stop-backend-simple.bat`

### للمحترفين:
1. ثبّت **PM2** (كـ Administrator)
2. استخدم `start-backend.bat`
3. فعّل بدء التشغيل مع Windows: `pm2 startup`

### للتطوير:
1. استخدم **التشغيل البسيط**
2. `start-backend-simple.bat`
3. ترى Logs مباشرة

---

## 📚 المزيد من المعلومات

- 📖 `BACKEND_ALWAYS_RUNNING.md` - دليل PM2 الكامل
- 📖 `PM2_QUICK_START.md` - دليل PM2 السريع

---

**تاريخ الإنشاء**: 2026-02-17  
**الحالة**: ✅ جاهز للاستخدام
