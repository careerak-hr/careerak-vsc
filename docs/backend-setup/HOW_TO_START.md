# 🚀 كيفية تشغيل Backend بشكل دائم

## ✅ Backend يعمل الآن!

تم تشغيل Backend بنجاح على المنفذ 5000 باستخدام PM2.

---

## 📊 التحقق من الحالة

```bash
pm2 status
```

**النتيجة المتوقعة:**
```
┌────┬──────────────────┬─────────┬──────┬──────────┐
│ id │ name             │ mode    │ ↺    │ status   │
├────┼──────────────────┼─────────┼──────┼──────────┤
│ 0  │ careerak-backend │ cluster │ 0    │ online   │
└────┴──────────────────┴─────────┴──────┴──────────┘
```

---

## 🔍 فحص حالة Backend

### الطريقة 1: استخدام deploy-check.bat
```bash
.\deploy-check.bat
```

### الطريقة 2: المتصفح
```
http://localhost:5000/health
```

### الطريقة 3: PM2
```bash
pm2 status
pm2 logs careerak-backend
```

---

## 🔍 عرض Logs

```bash
pm2 logs careerak-backend
```

أو عرض آخر 50 سطر:
```bash
pm2 logs careerak-backend --lines 50
```

---

## 🛑 إيقاف Backend

```bash
pm2 stop careerak-backend
```

---

## 🔄 إعادة تشغيل Backend

```bash
pm2 restart careerak-backend
```

---

## 🗑️ حذف من PM2

```bash
pm2 delete careerak-backend
```

---

## 🔧 إعداد بدء التشغيل التلقائي مع Windows

### الخطوة 1: فتح Command Prompt كـ Administrator
1. اضغط `Win + X`
2. اختر "Windows PowerShell (Admin)" أو "Command Prompt (Admin)"

### الخطوة 2: تنفيذ أمر startup
```bash
cd D:\Careerak\Careerak-vsc\backend
pm2 startup
```

### الخطوة 3: نسخ وتنفيذ الأمر
سيظهر أمر مثل:
```bash
pm2 startup windows -u "Eng. Alaa Uddien" --hp "C:\Users\Eng. Alaa Uddien"
```

انسخ الأمر كاملاً ونفذه.

### الخطوة 4: حفظ التكوين
```bash
pm2 save
```

### ✅ النتيجة
الآن Backend سيبدأ تلقائياً عند تشغيل Windows!

---

## 🧪 اختبار Backend

### الطريقة 1: المتصفح
افتح: `http://localhost:5000/health`

### الطريقة 2: PowerShell
```powershell
Invoke-RestMethod -Uri http://localhost:5000/health
```

### الطريقة 3: اختبار MongoDB
```bash
node test-mongodb.js
```

---

## 📋 الأوامر المفيدة

```bash
# عرض معلومات تفصيلية
pm2 show careerak-backend

# مراقبة الأداء
pm2 monit

# عرض جميع العمليات
pm2 list

# إعادة تحميل (بدون downtime)
pm2 reload careerak-backend

# حذف جميع العمليات
pm2 delete all

# إيقاف جميع العمليات
pm2 stop all
```

---

## 🔧 حل المشاكل

### Backend لا يعمل؟

```bash
# 1. فحص الحالة
pm2 status

# 2. فحص Logs
pm2 logs careerak-backend --lines 100

# 3. إعادة تشغيل
pm2 restart careerak-backend

# 4. إذا استمرت المشكلة، احذف وأعد التشغيل
pm2 delete careerak-backend
pm2 start ecosystem.config.js --name careerak-backend
pm2 save
```

### المنفذ 5000 مستخدم؟

```bash
# Windows
netstat -ano | findstr :5000

# إيقاف العملية
taskkill /PID <PID> /F
```

### PM2 لا يعمل بعد إعادة تشغيل Windows؟

```bash
# تحقق من startup
pm2 startup

# إذا لم يعمل، أعد الإعداد
pm2 unstartup
pm2 startup
# انسخ ونفذ الأمر الذي يظهر
pm2 save
```

---

## 📁 الملفات المهمة

- `ecosystem.config.js` - تكوين PM2
- `logs/pm2-error.log` - سجل الأخطاء
- `logs/pm2-out.log` - سجل الإخراج
- `logs/pm2-combined.log` - السجل الكامل
- `.env` - متغيرات البيئة

---

## 🌐 الروابط

- Backend: `http://localhost:5000`
- Health Check: `http://localhost:5000/health`
- API Docs: راجع `docs/API.md`

---

## 💡 نصائح

1. **لا تغلق نافذة PM2** - Backend يعمل في الخلفية
2. **استخدم `pm2 logs`** - لمتابعة ما يحدث
3. **احفظ التكوين دائماً** - بعد أي تغيير: `pm2 save`
4. **راقب الذاكرة** - استخدم `pm2 monit`

---

## 📚 المزيد من المعلومات

- 📄 `QUICK_START.md` - دليل سريع
- 📄 `docs/BACKEND_PERMANENT_RUNNING.md` - دليل شامل
- 📄 `PM2_QUICK_START.md` - دليل PM2
- 📄 [PM2 Documentation](https://pm2.keymetrics.io/)

---

**Backend يعمل الآن بشكل دائم! 🎉**

**آخر تحديث**: 2026-02-17
