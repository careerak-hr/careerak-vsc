# 🚀 تشغيل Backend بشكل دائم - Careerak

## 🎯 الهدف

تشغيل Backend بشكل دائم ومستمر دون الحاجة لتشغيله يدوياً كل مرة.

---

## 🔧 الحل: PM2 Process Manager

**PM2** هو مدير عمليات Node.js يضمن:
- ✅ تشغيل Backend بشكل دائم
- ✅ إعادة التشغيل التلقائي عند الأخطاء
- ✅ بدء التشغيل مع Windows
- ✅ مراقبة الأداء
- ✅ إدارة Logs

---

## 📦 التثبيت والإعداد

### الخطوة 1: تثبيت PM2 (مرة واحدة فقط)

```bash
npm install -g pm2
```

**أو استخدم الملف التلقائي:**
```bash
start-backend.bat
```
(سيثبت PM2 تلقائياً إذا لم يكن مثبتاً)

---

## 🚀 التشغيل

### الطريقة 1: استخدام الملف التلقائي (الأسهل)

```bash
start-backend.bat
```

**ماذا يفعل؟**
1. ✅ يتحقق من تثبيت PM2
2. ✅ يثبت PM2 إذا لم يكن مثبتاً
3. ✅ يوقف Backend القديم (إن وجد)
4. ✅ يشغّل Backend الجديد
5. ✅ يحفظ التكوين
6. ✅ يعد بدء التشغيل التلقائي مع Windows

### الطريقة 2: استخدام PM2 مباشرة

```bash
cd backend
pm2 start ecosystem.config.js --env production
pm2 save
```

---

## 🎛️ إدارة Backend

### ملفات الإدارة الجاهزة:

| الملف | الوظيفة |
|------|---------|
| `start-backend.bat` | تشغيل Backend بشكل دائم |
| `stop-backend.bat` | إيقاف Backend |
| `restart-backend.bat` | إعادة تشغيل Backend |
| `view-logs.bat` | عرض Logs |

### الأوامر المباشرة:

```bash
# عرض الحالة
pm2 status

# عرض Logs مباشرة
pm2 logs careerak-backend

# إيقاف
pm2 stop careerak-backend

# إعادة تشغيل
pm2 restart careerak-backend

# حذف
pm2 delete careerak-backend

# عرض معلومات تفصيلية
pm2 show careerak-backend

# مراقبة الأداء
pm2 monit
```

---

## 🔄 إعادة التشغيل التلقائي

### عند الأخطاء
PM2 يعيد تشغيل Backend تلقائياً عند:
- ❌ حدوث خطأ
- ❌ توقف غير متوقع
- ❌ استهلاك ذاكرة زائد (> 1GB)

### مع بدء Windows
```bash
# إعداد بدء التشغيل التلقائي
pm2 startup

# حفظ التكوين الحالي
pm2 save
```

**الآن Backend سيبدأ تلقائياً عند تشغيل Windows!**

---

## 📊 مراقبة Backend

### 1. عرض الحالة
```bash
pm2 status
```

**المخرجات:**
```
┌─────┬──────────────────┬─────────┬─────────┬──────────┬────────┐
│ id  │ name             │ mode    │ ↺      │ status   │ cpu    │
├─────┼──────────────────┼─────────┼─────────┼──────────┼────────┤
│ 0   │ careerak-backend │ fork    │ 0      │ online   │ 0.3%   │
└─────┴──────────────────┴─────────┴─────────┴──────────┴────────┘
```

### 2. عرض Logs
```bash
# آخر 50 سطر
pm2 logs careerak-backend --lines 50

# مباشر (live)
pm2 logs careerak-backend

# أو استخدم
view-logs.bat
```

### 3. مراقبة الأداء
```bash
pm2 monit
```

**يعرض:**
- استهلاك CPU
- استهلاك الذاكرة
- Logs مباشرة
- معلومات العملية

---

## 🔧 التكوين المتقدم

### ملف `ecosystem.config.js`:

```javascript
module.exports = {
  apps: [{
    name: 'careerak-backend',
    script: './src/index.js',
    instances: 1,
    autorestart: true,        // إعادة تشغيل تلقائي
    watch: false,             // مراقبة الملفات
    max_memory_restart: '1G', // إعادة تشغيل عند 1GB
    
    env: {
      NODE_ENV: 'production',
      PORT: 5000
    },
    
    // Logs
    error_file: './logs/pm2-error.log',
    out_file: './logs/pm2-out.log',
    log_file: './logs/pm2-combined.log',
    
    // إعادة التشغيل
    exp_backoff_restart_delay: 100,
    max_restarts: 10,
    min_uptime: '10s',
    
    // Cron لإعادة التشغيل اليومي (اختياري)
    // cron_restart: '0 0 * * *', // كل يوم 12 صباحاً
  }]
};
```

### تفعيل مراقبة الملفات (اختياري):

```javascript
watch: ['src'],
ignore_watch: ['node_modules', 'logs', 'uploads'],
```

**ملاحظة:** هذا يعيد تشغيل Backend عند تعديل أي ملف في `src/`

---

## 🧪 الاختبار

### 1. تشغيل Backend
```bash
start-backend.bat
```

### 2. التحقق من الحالة
```bash
pm2 status
```

**يجب أن ترى:**
```
status: online
```

### 3. اختبار API
```bash
curl http://localhost:5000/users
```

### 4. عرض Logs
```bash
pm2 logs careerak-backend --lines 20
```

**يجب أن ترى:**
```
✅ Pusher initialized successfully
📡 Pusher cluster: ap1
✅ MongoDB connected (first request)
```

---

## 🔄 سيناريوهات الاستخدام

### السيناريو 1: تشغيل لأول مرة
```bash
start-backend.bat
```

### السيناريو 2: تحديث الكود
```bash
# 1. إيقاف Backend
pm2 stop careerak-backend

# 2. تحديث الكود (git pull, etc.)
git pull

# 3. تثبيت dependencies الجديدة (إن وجدت)
npm install

# 4. إعادة تشغيل
pm2 restart careerak-backend

# أو ببساطة:
restart-backend.bat
```

### السيناريو 3: مشكلة في Backend
```bash
# عرض Logs
pm2 logs careerak-backend

# إعادة تشغيل
pm2 restart careerak-backend

# أو
restart-backend.bat
```

### السيناريو 4: إيقاف Backend
```bash
stop-backend.bat

# أو
pm2 stop careerak-backend
```

---

## 📋 Checklist

### ✅ قبل التشغيل:
- [ ] Node.js مثبت
- [ ] npm مثبت
- [ ] dependencies مثبتة (`npm install`)
- [ ] `.env` موجود ومُعد

### ✅ بعد التشغيل:
- [ ] `pm2 status` يظهر `online`
- [ ] `curl http://localhost:5000/users` يعمل
- [ ] Logs لا تحتوي على أخطاء
- [ ] MongoDB متصل (بعد أول طلب)
- [ ] Pusher مفعّل

---

## 🚨 استكشاف الأخطاء

### المشكلة 1: PM2 غير مثبت
```bash
npm install -g pm2
```

### المشكلة 2: Backend لا يبدأ
```bash
# عرض الأخطاء
pm2 logs careerak-backend --err

# حذف وإعادة التشغيل
pm2 delete careerak-backend
pm2 start ecosystem.config.js
```

### المشكلة 3: المنفذ 5000 مستخدم
```bash
# إيقاف العملية على المنفذ 5000
netstat -ano | findstr :5000
taskkill /PID <PID> /F

# أو غيّر المنفذ في .env
PORT=5001
```

### المشكلة 4: Backend يتوقف باستمرار
```bash
# عرض Logs
pm2 logs careerak-backend

# زيادة الذاكرة المسموحة
# في ecosystem.config.js:
max_memory_restart: '2G'
```

---

## 🎯 الفوائد

### مقارنة مع `npm start`:

| الميزة | npm start | PM2 |
|--------|-----------|-----|
| تشغيل دائم | ❌ | ✅ |
| إعادة تشغيل تلقائي | ❌ | ✅ |
| بدء مع Windows | ❌ | ✅ |
| مراقبة الأداء | ❌ | ✅ |
| إدارة Logs | ❌ | ✅ |
| Clustering | ❌ | ✅ |

---

## 📚 موارد إضافية

- 📖 [PM2 Documentation](https://pm2.keymetrics.io/docs/)
- 📖 [PM2 Quick Start](https://pm2.keymetrics.io/docs/usage/quick-start/)
- 📖 [PM2 Process Management](https://pm2.keymetrics.io/docs/usage/process-management/)

---

## 🎉 الخلاصة

### الآن Backend:
- ✅ يعمل بشكل دائم
- ✅ يعيد التشغيل تلقائياً عند الأخطاء
- ✅ يبدأ مع Windows
- ✅ سهل الإدارة
- ✅ مراقبة كاملة

### الأوامر الأساسية:
```bash
# تشغيل
start-backend.bat

# إيقاف
stop-backend.bat

# إعادة تشغيل
restart-backend.bat

# عرض Logs
view-logs.bat

# الحالة
pm2 status
```

---

**تاريخ الإنشاء**: 2026-02-17  
**الحالة**: ✅ جاهز للاستخدام  
**الإصدار**: 1.0.0

🎉 **Backend الآن يعمل بشكل دائم ومستمر!** 🎉
