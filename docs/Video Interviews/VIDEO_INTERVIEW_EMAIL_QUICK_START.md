# دليل البدء السريع - نظام البريد الإلكتروني لمقابلات الفيديو

## 🚀 البدء السريع (5 دقائق)

### 1. الميزات الرئيسية
- ✅ دعوات مقابلات فيديو احترافية
- ✅ تذكيرات تلقائية (24 ساعة و15 دقيقة)
- ✅ Cron jobs تلقائية
- ✅ 17 اختبار شامل

### 2. الاستخدام الأساسي

**إرسال دعوة مقابلة**:
```javascript
const { sendVideoInterviewInvitation } = require('../services/emailService');

await sendVideoInterviewInvitation(appointment, videoInterview, participants);
```

**إرسال تذكير**:
```javascript
const { sendVideoInterviewReminder } = require('../services/emailService');

// قبل 24 ساعة
await sendVideoInterviewReminder(appointment, videoInterview, participant, 1440);

// قبل 15 دقيقة
await sendVideoInterviewReminder(appointment, videoInterview, participant, 15);
```

### 3. Cron Jobs

**بدء التلقائي**:
```javascript
const { startReminderJobs } = require('./jobs/videoInterviewReminderCron');
startReminderJobs();
```

**الجدولة**:
- تذكير 24 ساعة: كل ساعة
- تذكير 15 دقيقة: كل دقيقة

### 4. الاختبار

```bash
cd backend
npm test -- videoInterviewEmail.test.js
```

**النتيجة المتوقعة**: ✅ 17/17 اختبارات نجحت

### 5. التكامل التلقائي

النظام يعمل تلقائياً عند:
- ✅ إنشاء موعد جديد
- ✅ إعادة جدولة موعد
- ✅ قبل 24 ساعة من المقابلة
- ✅ قبل 15 دقيقة من المقابلة

### 6. محتوى البريد

**الدعوة تحتوي على**:
- رابط المقابلة الفريد
- تفاصيل المقابلة (عنوان، وصف، تاريخ، وقت، مدة)
- 5 نصائح للمقابلة
- ملاحظات إضافية (إن وجدت)

**التذكير يحتوي على**:
- رابط المقابلة
- الوقت المتبقي
- تفاصيل المقابلة

### 7. الإعداد للإنتاج

**المتغيرات البيئية**:
```env
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
EMAIL_FROM=noreply@careerak.com
FRONTEND_URL=https://careerak.com
```

**تفعيل الإرسال الحقيقي**:
- قم بإلغاء التعليق على كود nodemailer في `emailService.js`
- أضف المتغيرات البيئية
- أعد تشغيل السيرفر

### 8. المراقبة

```bash
# عرض سجلات البريد
tail -f backend/logs/combined.log | grep "Email"

# عرض سجلات التذكيرات
tail -f backend/logs/combined.log | grep "reminder"

# عرض الأخطاء
tail -f backend/logs/error.log
```

### 9. استكشاف الأخطاء

**البريد لا يُرسل؟**
- تحقق من NODE_ENV (يجب أن يكون 'development' أو 'test' للمحاكاة)
- تحقق من المتغيرات البيئية في الإنتاج
- تحقق من سجلات الأخطاء

**Cron jobs لا تعمل؟**
- تحقق من أن السيرفر يعمل
- تحقق من سجلات السيرفر
- تحقق من أن NODE_ENV ليس 'test'

**الاختبارات تفشل؟**
- تأكد من أن NODE_ENV='test'
- شغّل `npm install` للتأكد من التبعيات
- تحقق من اتصال قاعدة البيانات

### 10. الملفات المهمة

```
backend/src/
├── services/emailService.js              # خدمة البريد
├── jobs/videoInterviewReminderCron.js    # Cron jobs
├── controllers/appointmentController.js  # التكامل
└── tests/videoInterviewEmail.test.js     # الاختبارات
```

## 📚 التوثيق الكامل

للمزيد من التفاصيل، راجع:
- 📄 `docs/VIDEO_INTERVIEW_EMAIL_INTEGRATION.md` - دليل شامل

---

**تاريخ الإنشاء**: 2026-03-02  
**الحالة**: ✅ جاهز للاستخدام
