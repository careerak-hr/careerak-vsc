# المقابلات الجماعية - دليل البدء السريع

## 🚀 البدء في 5 دقائق

### 1. Backend Setup (دقيقة واحدة)

```bash
cd backend

# تثبيت التبعيات (إذا لم تكن مثبتة)
npm install socket.io

# تشغيل السيرفر
npm run dev:socket
```

### 2. Frontend Setup (دقيقة واحدة)

```bash
cd frontend

# تثبيت التبعيات (إذا لم تكن مثبتة)
npm install socket.io-client

# تشغيل التطبيق
npm start
```

### 3. استخدام المكون (دقيقة واحدة)

```jsx
import GroupVideoCall from './components/VideoInterview/GroupVideoCall';

function MyInterviewPage() {
  return (
    <GroupVideoCall
      roomId="room-123"
      userId="user-456"
      userName="أحمد محمد"
      isHost={true}
      maxParticipants={10}
      onLeave={() => console.log('Left')}
    />
  );
}
```

### 4. اختبار (دقيقتان)

```bash
cd backend
npm test -- groupVideoInterview.test.js
```

**النتيجة المتوقعة**: ✅ 10/10 اختبارات نجحت

---

## 🎯 الميزات الرئيسية

### للمستخدمين العاديين
- ✅ الانضمام لمقابلة جماعية (حتى 10 مشاركين)
- ✅ عرض شبكي أو عرض المتحدث
- ✅ كتم/تشغيل الصوت والفيديو
- ✅ مشاركة الشاشة (مشارك واحد فقط)
- ✅ رفع اليد

### للمضيف فقط
- ✅ كتم جميع المشاركين
- ✅ إزالة مشارك من المقابلة

---

## 📡 Socket.IO Events (الأساسية)

### الانضمام للغرفة
```javascript
socket.emit('join-room', {
  roomId: 'room-123',
  userId: 'user-456',
  userName: 'أحمد محمد',
  maxParticipants: 10
});

socket.on('room-joined', (data) => {
  console.log('Joined!', data.participants);
});

socket.on('room-full', (data) => {
  alert('الغرفة ممتلئة!');
});
```

### كتم الجميع (مضيف فقط)
```javascript
socket.emit('mute-all', {
  roomId: 'room-123',
  hostId: 'host-user-id'
});
```

### إزالة مشارك (مضيف فقط)
```javascript
socket.emit('remove-participant', {
  roomId: 'room-123',
  hostId: 'host-user-id',
  targetSocketId: 'participant-socket-id'
});
```

---

## 🧪 اختبار سريع

### Test 1: دعم 10 مشاركين
```javascript
// يجب أن ينجح
for (let i = 1; i <= 10; i++) {
  socket.emit('join-room', { roomId, userId: `user-${i}`, ... });
}
// النتيجة: 10 مشاركين في الغرفة ✅
```

### Test 2: رفض المشارك الـ 11
```javascript
// يجب أن يُرفض
socket.emit('join-room', { roomId, userId: 'user-11', ... });
// النتيجة: room-full event ✅
```

---

## 🎨 التخصيص

### تغيير عدد الأعمدة
```css
/* في GroupVideoCall.css */
.video-grid {
  grid-template-columns: repeat(3, 1fr); /* 3 أعمدة */
}
```

### تغيير الألوان
```css
.control-btn {
  background: rgba(212, 129, 97, 0.3); /* نحاسي */
}
```

---

## 🔧 استكشاف الأخطاء

### المشكلة: "الكاميرا/الميكروفون غير متاح"
**الحل**: تأكد من منح الإذن في المتصفح

### المشكلة: "لا يمكن الاتصال بـ Socket.IO"
**الحل**: تحقق من `REACT_APP_SOCKET_URL` في `.env`

### المشكلة: "الفيديو لا يظهر"
**الحل**: تحقق من WebRTC peer connections في console

---

## 📚 المزيد من التفاصيل

راجع التوثيق الكامل: `docs/GROUP_VIDEO_INTERVIEWS.md`

---

**تاريخ الإنشاء**: 2026-03-02  
**الحالة**: ✅ جاهز للاستخدام
