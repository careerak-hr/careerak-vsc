# EmailValidator - دليل البدء السريع

## 🚀 البدء في 3 خطوات

### 1. الاستيراد

```jsx
import EmailValidator from './components/auth/EmailValidator';
```

### 2. الاستخدام

```jsx
function MyForm() {
  const [email, setEmail] = useState('');

  return (
    <EmailValidator
      value={email}
      onChange={setEmail}
      placeholder="أدخل البريد الإلكتروني"
      required
    />
  );
}
```

### 3. جاهز! ✅

---

## 📋 الخصائص الأساسية

| الخاصية | مطلوب | مثال |
|---------|-------|------|
| `value` | ✅ | `email` |
| `onChange` | ✅ | `setEmail` |
| `placeholder` | ❌ | `"أدخل البريد"` |
| `required` | ❌ | `true` |
| `debounceDelay` | ❌ | `500` (ms) |

---

## ✨ الميزات

- ✅ تحقق فوري أثناء الكتابة (debounced)
- ✅ أيقونات حالة (✓ / ✗ / ⏳)
- ✅ اقتراحات تصحيح الأخطاء
- ✅ رابط تسجيل الدخول للبريد الموجود
- ✅ دعم 3 لغات (ar, en, fr)

---

## 🧪 اختبار سريع

```bash
cd frontend
npm test -- EmailValidator.test.jsx --run
```

**النتيجة المتوقعة**: 11/11 ✅

---

## 🎯 أمثلة الاختبار

| الإدخال | النتيجة |
|---------|---------|
| `test@example.com` | ✓ متاح |
| `test@gmial.com` | ✗ هل تقصد gmail.com؟ |
| `notanemail` | ✗ صيغة غير صحيحة |
| `existing@example.com` | ✗ موجود + رابط تسجيل الدخول |

---

## 📚 التوثيق الكامل

للمزيد من التفاصيل، راجع:
- 📄 `docs/EMAIL_VALIDATOR_COMPONENT.md`
- 📄 `frontend/src/examples/EmailValidatorUsage.jsx`

---

## 🐛 مشكلة؟

1. تحقق من API endpoint: `/auth/check-email`
2. تحقق من `VITE_API_URL` في `.env`
3. افتح console للأخطاء

---

**تاريخ الإنشاء**: 2026-02-23
