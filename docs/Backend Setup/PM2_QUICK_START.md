# ⚡ PM2 Quick Start - Backend Careerak

## 🚀 التشغيل في 3 خطوات

```bash
# 1. الانتقال إلى مجلد Backend
cd backend

# 2. تشغيل السيرفر
npm run pm2:start

# 3. التحقق
npm run pm2:status
```

✅ **Done!** السيرفر الآن يعمل دائماً.

---

## 📊 الأوامر الأساسية

```bash
npm run pm2:start     # تشغيل
npm run pm2:status    # حالة
npm run pm2:logs      # سجلات
npm run pm2:restart   # إعادة تشغيل
npm run pm2:stop      # إيقاف
```

---

## ✅ التحقق من النجاح

```
┌────┬──────────────────┬──────┬──────┬─────────┬──────┬────────┐
│ id │ name             │ mode │ ↺    │ status  │ cpu  │ memory │
├────┼──────────────────┼──────┼──────┼─────────┼──────┼────────┤
│ 0  │ careerak-backend │ fork │ 0    │ online  │ 0%   │ 167mb  │
└────┴──────────────────┴──────┴──────┴─────────┴──────┴────────┘
```

**Status: online** = ✅ يعمل بنجاح!

---

## 🔧 استكشاف الأخطاء

### السيرفر لا يبدأ؟

```bash
# 1. تحقق من السجلات
npm run pm2:logs

# 2. تحقق من .env
type .env

# 3. أعد المحاولة
npm run pm2:restart
```

### إعادة تشغيل متكررة؟

```bash
# تحقق من الأخطاء
type logs\pm2-error.log
```

---

## 📚 المزيد من المعلومات

- 📄 **START_HERE.txt** - مرجع سريع
- 📄 **HOW_TO_START.md** - دليل التشغيل
- 📄 **QUICK_START.md** - دليل البدء السريع
- 📄 **docs/BACKEND_PERMANENT_RUNNING.md** - دليل شامل

---

**Backend الآن يعمل دائماً! 🎉**
