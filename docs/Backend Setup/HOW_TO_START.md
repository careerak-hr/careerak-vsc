# 🚀 كيفية تشغيل Backend

## ✅ الطريقة الصحيحة (PM2 - دائم)

```bash
cd backend
npm run pm2:start
```

**الفوائد**:
- ✅ يعمل دائماً (24/7)
- ✅ إعادة تشغيل تلقائي عند الأخطاء
- ✅ يبدأ تلقائياً مع Windows
- ✅ مراقبة الأداء والذاكرة
- ✅ سجلات منظمة

---

## 📊 الأوامر المفيدة

### التحقق من الحالة:
```bash
npm run pm2:status
```

### عرض السجلات:
```bash
npm run pm2:logs
```

### إعادة التشغيل:
```bash
npm run pm2:restart
```

### إيقاف السيرفر:
```bash
npm run pm2:stop
```

---

## ❌ الطريقة الممنوعة

**لا تستخدم هذه الأوامر**:
```bash
npm run dev        # ❌ ممنوع - تشغيل يدوي
npm start          # ❌ ممنوع - تشغيل يدوي
node server.js     # ❌ ممنوع - تشغيل يدوي
```

**لماذا ممنوع؟**
- ❌ يتوقف عند إغلاق Terminal
- ❌ لا إعادة تشغيل تلقائي
- ❌ لا مراقبة للأداء
- ❌ لا سجلات منظمة

---

## 📚 التوثيق الكامل

- 📄 `docs/BACKEND_NOW_RUNNING.md` - دليل شامل
- 📄 `docs/BACKEND_PERMANENT_RUNNING.md` - الإعداد الدائم
- 📄 `ecosystem.config.js` - تكوين PM2

---

**ملاحظة**: Backend الآن يعمل بشكل دائم ومستقر! ✅
