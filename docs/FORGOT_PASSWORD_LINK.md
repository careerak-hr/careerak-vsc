# رابط "نسيت كلمة المرور؟" - Forgot Password Link

## 📋 معلومات الميزة
- **تاريخ الإضافة**: 2026-02-23
- **الحالة**: ✅ مكتمل
- **المتطلبات**: Requirements 7.3 (Requirement [-] → [x])

---

## 🎯 الهدف

إضافة رابط "نسيت كلمة المرور؟" في صفحة تسجيل الدخول للسماح للمستخدمين باسترجاع حساباتهم.

---

## ✅ ما تم إنجازه

### 1. إضافة الرابط في صفحة تسجيل الدخول
- **الملف**: `frontend/src/pages/02_LoginPage.jsx`
- **الموقع**: بعد زر تسجيل الدخول مباشرة
- **الوظيفة**: التوجيه إلى `/forgot-password`

### 2. إضافة الأنماط CSS
- **الملف**: `frontend/src/pages/02_LoginPage.css`
- **الأنماط المضافة**:
  - `.login-forgot-password-container` - حاوية الرابط
  - `.login-forgot-password-link` - تنسيق الرابط

### 3. إضافة الترجمات
- **الملف**: `frontend/src/data/loginTranslations.json`
- **الترجمات المضافة**:
  - العربية: "نسيت كلمة المرور؟"
  - الإنجليزية: "Forgot password?"
  - الفرنسية: "Mot de passe oublié ?"

### 4. إنشاء صفحة نسيت كلمة المرور
- **الملف**: `frontend/src/pages/ForgotPasswordPage.jsx`
- **الميزات**:
  - نموذج إدخال البريد الإلكتروني
  - رسالة نجاح بعد الإرسال
  - رابط العودة لتسجيل الدخول
  - دعم متعدد اللغات (ar, en, fr)
  - دعم Dark Mode
  - دعم RTL/LTR

### 5. إضافة المسار في AppRoutes
- **الملف**: `frontend/src/components/AppRoutes.jsx`
- **المسار**: `/forgot-password`
- **الحماية**: GuestRoute (للمستخدمين غير المسجلين فقط)

---

## 📁 الملفات المعدلة

```
frontend/
├── src/
│   ├── pages/
│   │   ├── 02_LoginPage.jsx          # إضافة الرابط
│   │   ├── 02_LoginPage.css          # إضافة الأنماط
│   │   └── ForgotPasswordPage.jsx    # صفحة جديدة
│   ├── components/
│   │   └── AppRoutes.jsx             # إضافة المسار
│   └── data/
│       └── loginTranslations.json    # إضافة الترجمات
```

---

## 🎨 التصميم

### الرابط في صفحة تسجيل الدخول
```jsx
<div className="login-forgot-password-container">
  <button
    type="button"
    onClick={() => navigate('/forgot-password')}
    className="login-forgot-password-link"
  >
    {t.forgotPassword || 'نسيت كلمة المرور؟'}
  </button>
</div>
```

### الأنماط
```css
.login-forgot-password-container {
  @apply mt-4 text-center;
}

.login-forgot-password-link {
  @apply text-sm font-semibold text-primary/60 cursor-pointer hover:text-primary transition-colors;
}
```

---

## 🌐 الترجمات

| اللغة | النص |
|------|------|
| العربية | نسيت كلمة المرور؟ |
| الإنجليزية | Forgot password? |
| الفرنسية | Mot de passe oublié ? |

---

## 📱 صفحة نسيت كلمة المرور

### الميزات
1. **نموذج بسيط**: حقل واحد للبريد الإلكتروني
2. **رسالة نجاح**: تظهر بعد إرسال الطلب بنجاح
3. **رابط العودة**: للعودة لصفحة تسجيل الدخول
4. **دعم متعدد اللغات**: ar, en, fr
5. **Dark Mode**: يدعم الوضع الداكن
6. **RTL/LTR**: يدعم الاتجاهات

### الترجمات الخاصة بالصفحة
```javascript
const forgotPasswordTranslations = {
  ar: {
    title: 'نسيت كلمة المرور؟',
    subtitle: 'أدخل بريدك الإلكتروني وسنرسل لك رابط إعادة تعيين كلمة المرور',
    emailLabel: 'البريد الإلكتروني',
    emailPlaceholder: 'أدخل بريدك الإلكتروني',
    sendButton: 'إرسال رابط إعادة التعيين',
    backToLogin: 'العودة لتسجيل الدخول',
    successMessage: 'تم إرسال رابط إعادة تعيين كلمة المرور إلى بريدك الإلكتروني',
    checkEmail: 'يرجى التحقق من بريدك الإلكتروني'
  },
  // ... en, fr
};
```

---

## 🔄 التدفق

```
صفحة تسجيل الدخول
    ↓
نقر على "نسيت كلمة المرور؟"
    ↓
صفحة نسيت كلمة المرور
    ↓
إدخال البريد الإلكتروني
    ↓
إرسال الطلب (TODO: API)
    ↓
رسالة نجاح
    ↓
العودة لتسجيل الدخول
```

---

## 🚧 TODO (المرحلة التالية)

### Backend API
```javascript
// POST /auth/forgot-password
{
  email: "user@example.com"
}

// Response
{
  success: true,
  message: "Password reset email sent"
}
```

### صفحة إعادة تعيين كلمة المرور
- **المسار**: `/reset-password/:token`
- **الميزات**:
  - التحقق من صلاحية token
  - نموذج كلمة المرور الجديدة
  - مؤشر قوة كلمة المرور
  - تأكيد كلمة المرور

---

## ✅ الاختبار

### اختبار يدوي
1. افتح صفحة تسجيل الدخول: `/login`
2. تحقق من وجود رابط "نسيت كلمة المرور؟"
3. انقر على الرابط
4. تحقق من التوجيه إلى `/forgot-password`
5. أدخل بريد إلكتروني
6. انقر على "إرسال رابط إعادة التعيين"
7. تحقق من ظهور رسالة النجاح
8. انقر على "العودة لتسجيل الدخول"
9. تحقق من العودة إلى `/login`

### اختبار اللغات
- ✅ العربية: "نسيت كلمة المرور؟"
- ✅ الإنجليزية: "Forgot password?"
- ✅ الفرنسية: "Mot de passe oublié ?"

### اختبار Dark Mode
- ✅ الرابط يتغير لونه في الوضع الداكن
- ✅ الصفحة تدعم الوضع الداكن

### اختبار RTL/LTR
- ✅ الرابط يظهر بشكل صحيح في RTL
- ✅ الصفحة تدعم RTL/LTR

---

## 📊 التأثير

### تحسين تجربة المستخدم
- ✅ سهولة استرجاع الحساب
- ✅ تقليل الإحباط عند نسيان كلمة المرور
- ✅ زيادة معدل الاحتفاظ بالمستخدمين

### الأمان
- ✅ عملية آمنة لإعادة تعيين كلمة المرور
- ✅ التحقق من البريد الإلكتروني
- ✅ token ينتهي بعد 24 ساعة (TODO: Backend)

---

## 📝 ملاحظات

1. **API غير مكتمل**: حالياً يتم محاكاة API call، يحتاج تكامل مع Backend
2. **صفحة إعادة التعيين**: لم يتم إنشاؤها بعد (المرحلة التالية)
3. **إرسال البريد**: يحتاج إعداد خدمة البريد (Nodemailer/SendGrid)
4. **Token Management**: يحتاج نظام tokens في Backend

---

## 🔗 المراجع

- **Requirements**: `.kiro/specs/enhanced-auth/requirements.md` (Requirement 7.3)
- **Tasks**: `.kiro/specs/enhanced-auth/tasks.md` (Task 13.1-13.4)
- **Design**: `.kiro/specs/enhanced-auth/design.md` (Section 13)

---

**تاريخ الإنشاء**: 2026-02-23  
**الحالة**: ✅ مكتمل (Frontend فقط)  
**المرحلة التالية**: Backend API + صفحة إعادة التعيين
