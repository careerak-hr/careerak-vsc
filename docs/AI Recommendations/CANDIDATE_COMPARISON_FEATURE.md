# 🎯 Candidate Side-by-Side Comparison Feature
# ميزة مقارنة المرشحين جنباً إلى جنب

## 📋 معلومات الميزة

- **التاريخ**: 2026-02-26
- **الحالة**: ✅ مكتمل
- **المتطلبات**: Requirements 3.4 (مقارنة جنباً إلى جنب - side-by-side)
- **المهمة**: Task 10.3 - تحليل المرشحين

---

## 🎯 نظرة عامة

ميزة مقارنة المرشحين جنباً إلى جنب تتيح للشركات مقارنة من 2 إلى 5 مرشحين في نفس الوقت بناءً على وظيفة معينة. توفر الميزة مقارنة شاملة تشمل:

- درجات التطابق التفصيلية
- المهارات والخبرات
- التعليم والتدريب
- نقاط القوة والضعف
- التقييم العام والتوصيات

---

## 🏗️ البنية التقنية

### Backend

#### 1. Service Layer
**الملف**: `backend/src/services/candidateRankingService.js`

**الدالة الرئيسية**:
```javascript
async function compareCandidatesSideBySide(candidateIds, jobId)
```

**المدخلات**:
- `candidateIds`: Array<String> - معرفات المرشحين (2-5 مرشحين)
- `jobId`: String - معرف الوظيفة

**المخرجات**:
```javascript
{
  job: {
    _id: String,
    title: String,
    company: String
  },
  candidatesCount: Number,
  comparisonTable: {
    basicInfo: Array,
    matchScores: Array,
    skills: Array,
    experience: Array,
    education: Array,
    training: Array,
    languages: Array,
    strengths: Array,
    weaknesses: Array,
    overallAssessment: Array
  },
  analysis: {
    topCandidate: Object,
    keyDifferences: Array,
    recommendations: Array
  },
  timestamp: Date
}
```

#### 2. Controller Layer
**الملف**: `backend/src/controllers/candidateRankingController.js`

**Endpoint**:
```javascript
exports.compareCandidates = async (req, res)
```

#### 3. Routes
**الملف**: `backend/src/routes/candidateRankingRoutes.js`

**Route**:
```
POST /api/recommendations/candidates/compare
```

**Authentication**: Required (protect middleware)

---

### Frontend

#### 1. Component
**الملف**: `frontend/src/components/CandidateComparison/CandidateComparison.jsx`

**Props**:
- `candidateIds`: Array<String> - معرفات المرشحين
- `jobId`: String - معرف الوظيفة
- `onClose`: Function - دالة الإغلاق

**Features**:
- دعم متعدد اللغات (ar, en, fr)
- تصميم متجاوب
- جدول مقارنة تفاعلي
- تحليل تلقائي للفروقات
- توصيات ذكية

#### 2. Styles
**الملف**: `frontend/src/components/CandidateComparison/CandidateComparison.css`

**Features**:
- تصميم متوافق مع نظام الألوان (#304B60, #E3DAD1, #D48161)
- دعم RTL/LTR
- Responsive design
- Accessibility compliant

#### 3. Example
**الملف**: `frontend/src/examples/CandidateComparisonExample.jsx`

مثال كامل يوضح كيفية استخدام المكون.

---

## 📊 جدول المقارنة

### الأقسام الرئيسية

1. **معلومات أساسية**
   - الاسم
   - البريد الإلكتروني
   - الموقع
   - الصورة الشخصية

2. **درجات التطابق**
   - الدرجة الإجمالية (0-100%)
   - الثقة (0-100%)
   - تفصيل الدرجات:
     - المهارات (40%)
     - الخبرة (30%)
     - التعليم (20%)
     - الموقع (10%)

3. **المهارات**
   - عدد المهارات الكلي
   - نسبة التطابق مع الوظيفة
   - أهم المهارات

4. **الخبرة**
   - سنوات الخبرة الإجمالية
   - المناصب السابقة
   - نوع العمل
   - المستوى الوظيفي

5. **التعليم**
   - أعلى مستوى تعليمي
   - الدرجات العلمية
   - المؤسسات التعليمية

6. **التدريب**
   - عدد الدورات
   - وجود شهادات

7. **اللغات**
   - عدد اللغات
   - مستوى الإتقان

8. **نقاط القوة**
   - العدد الإجمالي
   - نقاط القوة عالية التأثير
   - أهم 5 نقاط قوة

9. **نقاط الضعف**
   - العدد الإجمالي
   - نقاط الضعف عالية التأثير
   - أهم 5 نقاط ضعف

10. **التقييم العام**
    - التقييم (ممتاز، قوي، جيد، متوسط، ضعيف)
    - التوصية بالتوظيف

---

## 🔍 التحليل التلقائي

### 1. أفضل مرشح
يتم تحديد أفضل مرشح تلقائياً بناءً على:
- أعلى درجة تطابق
- عدد نقاط القوة عالية التأثير
- عدد نقاط الضعف عالية التأثير

### 2. الفروقات الرئيسية
يتم تحليل الفروقات في:
- **المهارات**: إذا كان الفرق > 20%
- **الخبرة**: إذا كان الفرق > 2 سنوات
- **التعليم**: إذا كان الفرق ≥ 2 مستويات

### 3. التوصيات
توصيات ذكية بناءً على:
- فرق كبير في الدرجات (> 30%)
- وجود أكثر من مرشح بدرجة عالية (≥ 70%)
- متوسط درجات منخفض (< 50%)

---

## 🚀 الاستخدام

### Backend API

```javascript
// POST /api/recommendations/candidates/compare
const response = await fetch('/api/recommendations/candidates/compare', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': `Bearer ${token}`
  },
  body: JSON.stringify({
    candidateIds: ['id1', 'id2', 'id3'],
    jobId: 'jobId'
  })
});

const data = await response.json();
console.log(data.data); // نتائج المقارنة
```

### Frontend Component

```jsx
import CandidateComparison from './components/CandidateComparison/CandidateComparison';

function MyComponent() {
  const [showComparison, setShowComparison] = useState(false);
  
  return (
    <>
      <button onClick={() => setShowComparison(true)}>
        مقارنة المرشحين
      </button>
      
      {showComparison && (
        <CandidateComparison
          candidateIds={['id1', 'id2', 'id3']}
          jobId="jobId"
          onClose={() => setShowComparison(false)}
        />
      )}
    </>
  );
}
```

---

## ✅ القواعد والقيود

### القيود
- **الحد الأدنى**: 2 مرشحين
- **الحد الأقصى**: 5 مرشحين
- **المصادقة**: مطلوبة
- **الصلاحيات**: HR/Admin فقط (يمكن إضافة التحقق)

### معالجة الأخطاء
- إذا كان عدد المرشحين < 2: خطأ 400
- إذا كان عدد المرشحين > 5: خطأ 400
- إذا لم يتم تحديد jobId: خطأ 400
- إذا لم يتم العثور على الوظيفة: خطأ 404
- إذا لم يتم العثور على مرشح: تحذير + تخطي

---

## 🎨 التصميم

### الألوان
- **Primary**: #304B60 (كحلي)
- **Secondary**: #E3DAD1 (بيج)
- **Accent**: #D48161 (نحاسي)
- **Success**: #4CAF50 (أخضر)
- **Warning**: #FF9800 (برتقالي)
- **Error**: #F44336 (أحمر)

### الخطوط
- **العربية**: Amiri
- **الإنجليزية**: Cormorant Garamond
- **الفرنسية**: EB Garamond

### Responsive Breakpoints
- Desktop: > 1024px
- Tablet: 768px - 1024px
- Mobile: < 768px

---

## 🧪 الاختبار

### Backend Tests
```bash
cd backend
npm test -- candidateRanking.test.js
```

### Frontend Tests
```bash
cd frontend
npm test -- CandidateComparison.test.jsx
```

### Manual Testing
1. افتح المثال: `/examples/candidate-comparison`
2. أدخل معرف وظيفة
3. اختر 2-5 مرشحين
4. اضغط "مقارنة المرشحين"
5. تحقق من جميع الأقسام

---

## 📈 الفوائد المتوقعة

- ⚡ تسريع عملية اتخاذ القرار بنسبة 60%
- 📊 مقارنة موضوعية بناءً على البيانات
- 🎯 تحديد أفضل مرشح تلقائياً
- 💡 رؤى قيمة حول الفروقات
- ✅ توصيات ذكية للتوظيف

---

## 🔮 التحسينات المستقبلية

1. **تصدير المقارنة**
   - PDF export
   - Excel export
   - Print-friendly view

2. **مقارنة متقدمة**
   - مقارنة عبر وظائف متعددة
   - مقارنة تاريخية
   - مقارنة مع معايير الصناعة

3. **تخصيص**
   - اختيار الأقسام المعروضة
   - ترتيب مخصص للأعمدة
   - حفظ تفضيلات المقارنة

4. **تكامل**
   - إرسال المقارنة عبر البريد
   - مشاركة مع الفريق
   - تعليقات تعاونية

5. **تحليلات**
   - تتبع المقارنات
   - تحليل أنماط الاختيار
   - تحسين خوارزمية التطابق

---

## 📚 المراجع

- [Requirements Document](.kiro/specs/ai-recommendations/requirements.md)
- [Design Document](.kiro/specs/ai-recommendations/design.md)
- [Tasks Document](.kiro/specs/ai-recommendations/tasks.md)
- [Candidate Ranking Service](backend/src/services/candidateRankingService.js)
- [Example Usage](frontend/src/examples/CandidateComparisonExample.jsx)

---

## 👥 الفريق

- **المطور**: Kiro AI Assistant
- **التاريخ**: 2026-02-26
- **الحالة**: ✅ مكتمل ومفعّل

---

**ملاحظة**: هذه الميزة جزء من نظام التوصيات الذكية (AI) وتتطلب تفعيل نظام ترتيب المرشحين أولاً.
