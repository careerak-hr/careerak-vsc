# 🚀 نظام التوصيات اليومية - دليل البدء السريع

## ⚡ البدء في 5 دقائق

### 1. تثبيت التبعيات (30 ثانية)

```bash
cd backend
npm install node-cron
```

### 2. تشغيل السيرفر (10 ثواني)

```bash
npm start
```

سيبدأ Cron Job تلقائياً! ✅

### 3. التحقق من الجدولة (10 ثواني)

ابحث في console السيرفر عن:
```
✅ تم بدء جدولة التحديث اليومي للتوصيات
🕐 بدء جدولة التحديث اليومي للتوصيات...
📅 الجدول: 0 2 * * * (Africa/Cairo)
```

---

## 🧪 اختبار سريع

### اختبار 1: تشغيل يدوي (دقيقة واحدة)

```bash
# Postman أو curl
POST http://localhost:5000/api/recommendations/daily-update
Authorization: Bearer <admin_token>
Content-Type: application/json

{
  "batchSize": 5,
  "maxUsers": 10
}
```

### اختبار 2: جلب التوصيات الجديدة (30 ثانية)

```bash
GET http://localhost:5000/api/recommendations/new?limit=5
Authorization: Bearer <user_token>
```

### اختبار 3: عرض في Frontend (دقيقة واحدة)

```jsx
// في HomePage.jsx
import NewForYou from './components/NewForYou/NewForYou';

function HomePage() {
  return (
    <div>
      <NewForYou />
    </div>
  );
}
```

---

## 📊 النتيجة المتوقعة

### في Console السيرفر:
```
🚀 بدء التحديث اليومي للتوصيات...
👥 عدد المستخدمين النشطين: 10
💼 عدد الوظائف النشطة: 50
⚙️ معالجة الدفعة 1/2 (5 مستخدم)...
⚙️ معالجة الدفعة 2/2 (5 مستخدم)...
✅ اكتمل التحديث اليومي للتوصيات
📊 الإحصائيات:
   - المستخدمون المعالجون: 10/10
   - إجمالي التوصيات: 200
   - المدة: 15 ثانية
```

### في Frontend:
- قسم "جديد لك" 🆕
- 5-10 توصيات جديدة
- علامة "NEW" على كل توصية
- نسبة التطابق لكل وظيفة
- أسباب التوصية

---

## 🔧 التخصيص السريع

### تغيير وقت التشغيل:

```javascript
// backend/src/jobs/dailyRecommendationCron.js
this.schedule = '0 3 * * *'; // الساعة 3:00 صباحاً
```

### تغيير عدد التوصيات:

```javascript
// backend/src/services/dailyRecommendationService.js
limit: 30 // بدلاً من 20
```

---

## 🐛 استكشاف الأخطاء السريع

### المشكلة: لا توجد توصيات جديدة

**الحل السريع**:
```bash
# تشغيل يدوي
POST /api/recommendations/daily-update
```

### المشكلة: Cron Job لا يعمل

**الحل السريع**:
```bash
# أعد تشغيل السيرفر
npm restart
```

---

## 📚 التوثيق الكامل

للمزيد من التفاصيل، راجع:
- 📄 `docs/DAILY_RECOMMENDATIONS_SYSTEM.md` - دليل شامل (50+ صفحة)

---

## ✅ Checklist

- [ ] تثبيت node-cron
- [ ] تشغيل السيرفر
- [ ] التحقق من console
- [ ] اختبار التشغيل اليدوي
- [ ] اختبار جلب التوصيات
- [ ] إضافة مكون NewForYou
- [ ] اختبار في المتصفح

---

**وقت الإعداد الإجمالي**: 5 دقائق ⚡

**تاريخ الإنشاء**: 2026-02-27
