package com.careerak.app;

import com.getcapacitor.Plugin;
import com.getcapacitor.annotation.CapacitorPlugin;

@CapacitorPlugin(name = "WebViewConfig")
public class WebViewConfigPlugin extends Plugin {
    // This plugin can be left empty if its only purpose is to be registered in MainActivity
    // It serves as a placeholder for any future webview-related native configurations.
}
