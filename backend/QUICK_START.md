# ⚡ دليل البدء السريع - Backend Careerak

## 🎯 التشغيل في 30 ثانية

```bash
# 1. الانتقال إلى مجلد Backend
cd backend

# 2. تشغيل السيرفر (دائم مع PM2)
npm run pm2:start

# 3. التحقق من الحالة
npm run pm2:status
```

**النتيجة المتوقعة**:
```
┌────┬──────────────────┬──────┬──────┬─────────┬──────┬────────┐
│ id │ name             │ mode │ ↺    │ status  │ cpu  │ memory │
├────┼──────────────────┼──────┼──────┼─────────┼──────┼────────┤
│ 0  │ careerak-backend │ fork │ 0    │ online  │ 0%   │ 167mb  │
└────┴──────────────────┴──────┴──────┴─────────┴──────┴────────┘
```

✅ **Status: online** = السيرفر يعمل بنجاح!

---

## 📋 الأوامر الأساسية

| الأمر | الوصف |
|------|-------|
| `npm run pm2:start` | تشغيل السيرفر |
| `npm run pm2:status` | التحقق من الحالة |
| `npm run pm2:logs` | عرض السجلات |
| `npm run pm2:restart` | إعادة التشغيل |
| `npm run pm2:stop` | إيقاف السيرفر |

---

## 🔍 استكشاف الأخطاء

### المشكلة: السيرفر لا يبدأ

**الحل**:
```bash
# 1. تحقق من المنفذ 5000
netstat -ano | findstr :5000

# 2. أوقف العملية القديمة
npm run pm2:stop

# 3. ابدأ من جديد
npm run pm2:start
```

### المشكلة: أخطاء في السجلات

**الحل**:
```bash
# عرض آخر 50 سطر من السجلات
npm run pm2:logs -- --lines 50

# عرض الأخطاء فقط
type logs\pm2-error.log
```

### المشكلة: استهلاك ذاكرة عالي

**الحل**:
```bash
# إعادة تشغيل السيرفر
npm run pm2:restart
```

---

## 🌐 الوصول إلى السيرفر

- **URL المحلي**: http://localhost:5000
- **API Health Check**: http://localhost:5000/api/health
- **Admin Dashboard**: http://localhost:5000/admin

---

## 📁 الملفات المهمة

```
backend/
├── .env                    # المتغيرات البيئية (سري)
├── ecosystem.config.js     # تكوين PM2
├── src/
│   └── index.js           # نقطة البداية
└── logs/
    ├── pm2-out.log        # سجل الإخراج
    ├── pm2-error.log      # سجل الأخطاء
    └── pm2-combined.log   # السجل المدمج
```

---

## ⚙️ المتغيرات البيئية المطلوبة

تأكد من وجود هذه المتغيرات في `.env`:

```env
PORT=5000
MONGODB_URI=mongodb+srv://...
JWT_SECRET=your_secret_key
NODE_ENV=development
OAUTH_ENCRYPTION_KEY=your_32_byte_hex_key
```

**توليد OAUTH_ENCRYPTION_KEY**:
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

---

## 🔐 الأمان

- ✅ لا تشارك `.env` على Git
- ✅ استخدم مفاتيح قوية
- ✅ غيّر المفاتيح الافتراضية
- ✅ راجع السجلات بانتظام

---

## 📚 التوثيق الكامل

- 📄 **START_HERE.txt** - مرجع سريع
- 📄 **HOW_TO_START.md** - دليل التشغيل
- 📄 **docs/BACKEND_NOW_RUNNING.md** - دليل شامل
- 📄 **docs/BACKEND_PERMANENT_RUNNING.md** - الإعداد الدائم

---

## ✅ قائمة التحقق

قبل البدء، تأكد من:

- [x] Node.js مثبت (v14+)
- [x] npm مثبت
- [x] PM2 مثبت عالمياً (`npm install -g pm2`)
- [x] MongoDB متاح (محلي أو Atlas)
- [x] ملف `.env` موجود ومكتمل
- [x] المنفذ 5000 متاح

---

## 🎉 الخلاصة

**Backend الآن جاهز للعمل!**

```bash
cd backend
npm run pm2:start
```

✅ يعمل دائماً (24/7)  
✅ إعادة تشغيل تلقائي  
✅ مراقبة الأداء  
✅ سجلات منظمة

**استمتع بالتطوير! 🚀**
